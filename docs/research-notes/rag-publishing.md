---
title: RAG 知识库赋能专业出版
tags: [RAG, 出版, 论文, 知识库]
---

# RAG 知识库赋能专业出版

> 这是一篇示例文档，演示「分类 + 标签」的写法。替换成你自己的内容即可。

## 摘要

垂直领域出版对内容精准度要求极高。RAG（检索增强生成）通过把领域知识库
接入大模型，在生成时检索权威资料，显著降低幻觉率。

## 核心要点

1. **知识库构建**：清洗、切分、向量化领域文献
2. **检索链路**：query 改写 → 混合检索 → 重排
3. **生成约束**：引用溯源、可核查

!!! tip "标签怎么来的"
    本文头部的 `tags: [RAG, 出版, 论文, 知识库]` 会被自动汇总到
    [标签总览](../../tags/) 页面，点进去能看到所有带该标签的文章。

```python
# 示例：最简单的检索片段
def retrieve(query, index, top_k=3):
    return index.search(query, top_k)
```
